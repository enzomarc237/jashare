name: jashare
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  macos_ui: ^2.0.2
  macos_window_utils: ^1.4.0  # Add this line
  web_socket_channel: ^3.0.2
  path_provider: ^2.1.1
  file_picker: ^8.3.5
  provider: ^6.1.1
  permission_handler: ^11.0.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
